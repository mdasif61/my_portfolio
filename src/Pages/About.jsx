import { Box, Typography } from "@mui/material";

const About = () => {
    return (
        <Box sx={{backgroundColor:'#121212', padding:'50px'}} id="about">
            <Typography sx={{ color: 'white' }} variant="h1">About</Typography>
        </Box>
    );
};

export default About;